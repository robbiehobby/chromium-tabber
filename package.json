{
  "private": true,
  "scripts": {
    "watch": "node esbuild.config.mjs",
    "build": "tsc -noEmit && node esbuild.config.mjs build"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.10",
    "@types/chrome": "^0.0.297",
    "daisyui": "^4.12.23",
    "esbuild": "^0.25.0",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.3"
  }
}
